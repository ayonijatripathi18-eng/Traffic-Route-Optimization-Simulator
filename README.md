# Traffic-Route-Optimization-Simulator
An Artificial Intelligence project that simulates traffic route optimization using classical search algorithms. The city is modeled as a weighted graph, and BFS, DFS, and A* search are applied to compare uninformed and informed search strategies for path planning.
